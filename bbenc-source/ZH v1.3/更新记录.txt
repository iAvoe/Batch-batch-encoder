通过多项目拖拽选框实现了快速导入大量视频的代码块
实现了基于导入的所有视频直接判断大批量与单文件，是否启用文件名补零，以及异常值判断的功能
通过直接显示文件导入窗，然后通过字符匹配的方法来导入编码器和压制工具，减少了用户端的操作步骤
原本用于标记路径选项的两个变量改为序列0项储存路线，序列1项储存路径，从而减少了6个内部变量
Windows From文件或路径选择窗通过函数参数传递到窗口属性的方法在标题上显示出操作引导
基于以上改进，步骤2已经全部GUI化，且只需要鼠标操作即可完成全部的子步骤
合并了步骤2大批量以及单文件模式的两种批处理生成和导出代码，从而实现了步骤2单文件和打批量模式的合并
增加了步骤2A - 用于视频处理导致格式变更的上下游参数手动更改自检表
增加了为x264计算subme参数的函数功能
修复了自动计算transfer参数的参数名错误
步骤3中自动计算x264, x265, ffmpeg部分参数的功能合并到了步骤2，并减少了4个内部变量
SVFI线路下自动替换渲染配置文件的代码合并到了步骤2，并减少了两个内部变量
增加了SVFI命令行--pipe-out --pipe-colormatrix的自动生成（alpha版本 - 直接使用标准ffprobe colormatrix格式，可能会不兼容）
逻辑性变更，将导入的.vpy/.avs分别再导入对应的视频
逻辑性变更，支持同时导入所有上游压制软件，脚本自动根据导入的视频匹配选择不重复的压制软件
引入了srcOPS，impOPS两个class用于实现路径，文件名和文件后缀的拆分支持
引入了基于文件名+后缀识别删除，以及文件名搜索的功能，实现了与.vpy/.avs同目录下指向视频源的自动定位
引入了从用户导入文件列表中删除与导入.vpy/.avs脚本重复的视频文件的功能
增加了更详细的报错说明，包括新的报错函数以及定位用的代号
基于导入视频文件的分类和去重，改进了检测压制任务数量的计算，从而减少了出错概率
引入了检测视频源，脚本源，压制程序文件大小过小的警告，以便提早发现操作或系统错误
全脚本范围内统一了Pipe到ForEach-Object，而不是 Foreach ($_ in $xxx)的处理方法
~~引入了将方括号"[" "]"替换为"``[" "``]"的功能以兼容Get-Item函数~~ Get-Item -LiteralPath
重写了x265/HEVC中--pools参数的算法，改为根据X个任务以及Y个CPU节点生成多个主控批处理，每个主控批处理只调用一个CPU(设定HEVC压制批处理的--pools参数)以及等分的压制批处理范围，用户只需要同时运行所有压制批处理即可
增加了生成批处理的/node命令参数的算法，和--pools类似，用于确保整个压制pipe链路都运行在同一个CPU节点中
重写了步骤2A至2C的所有导入逻辑，包括ffmpeg，ffprobe，x264，x265路径的本地化，基于导入源文件的vspipe/avs2yuv/avs2pipemod导入启用/关闭策略
由于复杂度过高，所以将多个传递参数的序列改为class，从而定义了属性的变量类型以及名称
增加了多个报错提示 - 引入了extOPS, encOPS两个类
完成了ffprobe检测视频功能的函数的函数化，解决了Invoke-Expression函数无法处理带空格路径的问题
完成了色彩空间，位深，帧类型参数变量的生成代码段（步骤G）集成到class constructor的变更


开发中：步骤2E - 将压制模式, 上游程序, 下游程序中，根据上游程序（即导入源）的全部种类分别生成多个步骤2压制参数，并（步骤2F等）分别打印到批处理
开发中：每个视频分别进行ffprobe分析（跳过.vpy，.avs）并组合成序列范围，从而根据每个视频的参数独立计算x264/5参数值的函数
开发中：使ffprobe分析的步骤同时支持.vsp, .avs和视频源, 从而实现步骤2A至2H支持混合源输入的条件
